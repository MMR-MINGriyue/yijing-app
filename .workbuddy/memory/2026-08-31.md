# 2026-08-31 第十四轮迭代 — 部署准备 (GitHub Pages)

- 新增 `.github/workflows/deploy.yml`：push main / workflow_dispatch 触发；permissions（pages write + id-token write）+ concurrency（group pages，cancel-in-progress）；部署前用 node 内联脚本做语法冒烟（index.html 内联 script 提取 new Function / sw.js / data.js），与本地 check-syntax 同逻辑，已在本地验证可跑（1 inline OK / sw.js OK / data.js OK）；upload-pages-artifact path=. 整目录上传（index.html 首页约定）
- README 新增「部署 (GitHub Pages)」节：推 main → Settings Pages 选 GitHub Actions → 访问 https://<user>.github.io/<repo>/；说明全站相对路径（./）天然兼容子路径；附 Vercel/Netlify/nginx 直传方案；PWA 提示（SW+manifest 需 HTTPS）
- 文件结构清单补充 deploy.yml 与 .pwtest/
- 验证：工作流冒烟命令本地复跑通过 + iter13/10/8 三套回归 65 项全通过
- 注意：应用路径检查过均为相对路径（manifest shortcuts `./index.html#xxx`、SW CACHE_ASSETS `./`），子路径部署无需修改
- sw.js 保持 v1.13.0（无运行时代码变更）
- 状态：应用收尾完成。剩余可选项：真实推 GitHub 部署（需用户提供仓库）、后续 UX 打磨

# 2026-08-31 第十三轮迭代 — 06 屏方向筛选 + 07 屏图表跳转联动

- **06 屏方向筛选**：`dirFilterRow`（复用 hex-filter-chip 样式），chips 从当月记录动态生成（全方向 + 出现过的方向 + 计数）；无记录/无方向时整行隐藏；与卦象筛选 AND 复合（renderCurrentMonth 链式过滤）；`activeDirFilter` 切月自动重置；hexFilterClear 同时清两种筛选；updateFilterMeta 支持复合展示「「乾」 + 「事业」 · N 条」
- **07 屏图表跳转**：renderBars 加 onClick 参数（DOM 化构建，clickable 类 + role=button + 键盘支持）；方向条 → `YijingUI.filterByDirection(k)` + gotoScreen(5)；方式条 → 跳 06 屏并清除方向筛选（filterByDirection('')，避免残留上次的筛选态）
- 修复：方式条首版不清方向筛选导致跳转后仍显示旧筛选（iter13 断言抓住）
- 主题核查：--pine/--cinnabar 均在 :root 定义，无缺变量（me-bars 的 fallback 冗余无害）
- 验证：iter13 21/21 + 五轮回归（12:20 / 11:18 / 10:21 / 9:19 / 8:23）共 **122 项全通过**；语法检查通过
- sw.js → v1.13.0；README 同步（06 筛选 / 07 图表跳转 / API filterByDirection）
- 状态：13 轮迭代后 7 屏全部真交互真数据。下一迭代候选：起卦问题输入与方向组合的引导优化；06 屏筛选与搜索组合空态文案细化；如需收尾可做部署（GitHub Pages / 静态托管）

# 2026-08-31 第十二轮迭代 — 方向标签交互 + 分享卡干支落款 + 重置示例数据

- **02 屏方向标签修复**（审计发现的最后死元素：5 个 tag 点击永不切换激活，第 1 项永久 active）：
  - 点击/键盘切换激活态，写 `localStorage yijing.lastDirection`；重载恢复上次选择（找不到回退第 1 项）
  - a11y：row 加 role=radiogroup，tag 加 role=radio + aria-checked 同步
  - 修复恢复逻辑 bug：`i === 0 || d.name === last` 恒真导致首项永远 active，改为互斥 `(!hasLast && i === 0) || d.name === last`
- **分享卡干支落款**：YijingShare.draw 页脚升级为 `易 道 · 卦 象 解 读　丙午年 丙申月 丁丑日　2026-08-31`（letterSpacing 2px，Calendar 失败时静默回退纯公历）
- **设置面板「重置为示例数据」**：二次确认（独立 resetTimer，与 clear 的 confirmTimer 分离避免交叉），执行 `YijingHistory.save(YIJING_DATA.HISTORY.slice())` + refreshHistory；收藏不受影响；toast 报告；关闭面板时复位确认文案
- 验证：iter12 20/20 + 四轮回归（iter11 18 / iter10 21 / iter9 19 / iter8 23）共 **101 项全通过**；语法检查通过
- 测试技巧：页脚 canvas 文本验证用 `getImageData` 采样像素亮度；localStorage.clear() 必须在 page.goto 之后（about:blank 上会 SecurityError）
- sw.js → v1.12.0；README 同步
- 待办（下一迭代候选）：06 屏筛选 chips（按卦 no）可扩展按方向筛选；07 屏方向/方式条形图可点击跳转 06 屏；暗色主题变量核查（--pine 等自定义色）

# 2026-08-31 第十一轮迭代 — 导入 JSON + 月干支 + 快捷入口收尾

- 设置面板新增「导入历史数据」：隐藏 file input（accept=application/json），FileReader 解析导出格式 `{app, history, favorites}`；历史按 ts（或 name+question 组合键）去重合并后 `YijingHistory.save`；收藏取并集（校验 1-64）；导入后 dispatch favchange + refreshHistory + refreshMeta，toast 报告「新增 N 条 · N 卦收藏」；非法 JSON 报错提示
- 月干支（ganzhiMonth）：TERMS 表（小寒1/6丑 → 立春2/4寅 → … → 大雪12/7子）按公历年内查最近已至节气定月序（1月1-5日默认子上年）；年上起月法 `monthGanStart=[2,4,6,8,0][yearGan%5]`；首版跨年比较有 bug（小寒在年末循环里误判），重写为年内排序查表后 5 个日期断言全对（2024-02-10丙寅 / 2024-01-20乙丑 / 2024-12-25丙子 / 2026-08-31丙申 / 2025-03-10己卯）
- 问候日期行升级为年+月+日完整干支：`丙午年 丙申月 丁丑日 · 8月31日`
- quick-row 第三个快捷项「历史记录」补绑 gotoScreen(5)（前两项已有：随机→run('coin')、手动→跳屏聚焦输入框）
- 验证：iter11 18/18 + iter10 21/21 + iter9 19/19 + iter8 23/23 共 81 项全通过；语法检查通过
- 测试教训：干支断言先手算或查证再写（2024-01-20 实为癸卯年乙丑月，我预期写成甲子）；「清空再恢复」类用例要把 localStorage 相关键全部清掉（favHexes 漏清导致并集残留假失败）
- 月干支精度说明：2024-2030 用同一张表，节气年漂移 <1 天，UI 展示足够
- sw.js → v1.11.0；README 同步（导入功能/月干支/API 列表）
- 待办（下一迭代候选）：起卦方向（chips）与问题的持久化/统计细化；设置面板可加「重置为示例数据」；分享卡可含干支日期落款

# 2026-08-31 第十轮迭代 — 07 屏「我的」个人页

- 新增第 7 屏「我的」（phoneMe）：统计概览（起卦总数 / 连续天数 streak 按 ts 日聚合自今日或昨日向前 / 收藏数 / 始于最早真实记录日）、收藏卦横滑 chips（trigramU+trigramD 双卦符，点击 openDetail+gotoScreen(3)）、问卦方向与起卦方式分布条形图（仅真实记录，direction 用 directionColor 着色，method 映射 numeric/yarrow/coin）、历史记录/六十四卦快捷入口
- 导航接入：tab「我 的」data-goto 5→6；SCREEN_TO_TAB 加 6:3；HASH_MAP 加 me:6；appHint 屏数改为按 slides.length 动态；manifest shortcuts 加「我的」#me
- 设置面板双入口：bindSettingsSheet 重构为 querySelectorAll('#settingsBtn, #meSettingsBtn')，暴露 YijingUI.openSettings，关闭时归还焦点到触发按钮
- 联动：refreshHistory 调 refreshMe；yijing:favchange 触发重渲染
- 验证：iter10 21/21、iter9 19/19、iter8 23/23 全通过；语法检查通过
- 测试教训：跨午夜跑测试时「今日/昨日」类断言必须用 Date.now() 动态推期望值，不能写死日期（本次 8月29日→8月30日 假失败）
- sw.js → v1.10.0；manifest/README 同步
- 备注：iter9 的「6 屏状态栏都存在」断言 count>=6 在 7 屏下自然通过，无需改
- 待办（下一迭代候选）：月干支（需节气表）；设置面板可加「导入 JSON」与导出对称；quick-row 三个快捷项（手动起卦/随机起卦/历史记录）尚未绑事件

# 2026-08-31 第九轮迭代 — 时间真实性 + 最近占卜动态化

- 新增 `YijingCalendar` 模块（JDN 儒略日换算 + 干支纪日/纪年，锚点 JD 2458511=甲子日、1984=甲子年、立春为界）：JDN 公式经 2000-01-01=2451545 与 2024-01-01=2460311 双锚验证
- 01 屏问候日期行：`丙午年 · 丁丑日 · 8月31日`（真实干支+公历；时辰描述挪至 title）
- 状态栏 6 屏实时时钟（替代静态 9:41，30s 刷新）
- 历史记录完整时间戳：`buildHistoryRecord` 增加 `ts`；新增 `fmtRecordTime()`（今日/日期 + 时分，旧数据回退 date 字段）用于屏1/屏6 卡片；⇅ 排序与「本周」统计按 ts 计算（内置样例无 ts 沉底）
- 屏1「最近占卜」动态化：写死的需卦/咸卦卡改为 JS 渲染真实历史前 2 条（无记录降级内置样例），点击直达屏4 解析
- 修复 UX 缺口：屏3 卦卡、屏6 历史卡、屏1 最近卡点击后 `gotoScreen(3)` 自动跳转到解析屏（原仅渲染不跳屏，app 模式下无反馈）；屏4 顶栏 `←` 绑定返回——gotoScreen(3) 时记录来路屏到 `YijingUI._detailBackTo`，无记录时回 03 屏
- 修复 bug：renderStats 中 `const d` 重赋值导致加载即报 "Assignment to constant variable"
- 验证：Playwright iter9 19/19 通过（时钟/干支双锚/最近卡动态+跳屏+返回/ts 双向排序/清空降级）；iter8 回归 23/23 通过；语法检查通过
- 测试注意：data.js 内置样例前两条恰好是乾卦/需卦，断言示例降级时不要用卦名区分，用问题文本
- sw.js → v1.9.0；README 补时间真实性与导航说明；删除 debug-recent.js
- 待办（下一迭代候选）：月干支（需节气表，暂跳过避免不准）；「我的」tab 独立个人页

# 2026-08-31 第八轮迭代 — 死按钮收尾 + 屏6 全动态化

- 审计发现并修复最后 4 处「可点但无事件」死元素：
  - 屏1 `⚙` 设置按钮 → 新增底部抽屉设置面板（role=dialog/aria-modal/Esc 关闭/焦点管理）：导出历史数据（JSON 下载，含起卦记录+收藏）、清空历史记录（二次确认防误触，3 秒超时复位）
  - 屏6 `⇅` 排序按钮 → 「最新在前 / 最早在前」切换，按钮加 .flipped 翻转态，分组标签随之切换（近期/同月早些 ↔ 早些/同月近期）
  - 屏4 头部 `↗` → 触发与底部 detailShare 相同的分享卡生成（共享点击）
  - 屏6 搜索栏 → 从纯占位 div 改为真实 `<input>`，按卦名/问题/日期关键词过滤当月记录，无匹配显示「无 匹 配 记 录」
- 屏6 统计卡动态化：总次数/本周(近 7 天)/收藏( favHexes 数)全部实时计算，顶栏「共 N 次」同步；refreshHistory、yijing:favchange、清空操作均联动刷新
- 全局轻提示抽取为 window.yijingToast（原 bindShareCard 内部 toast 函数改为复用）
- 验证：Node 语法检查通过（124076 字符单块）；Playwright 23/23 项通过（Edge 渠道，App 390×844）：统计准确性/排序切换/搜索命中与空态/设置面板开合+二次确认+清空联动+Esc/头部分享触发/JSON 导出下载/全程无运行时错误
- 已知说明：无头环境 navigator.share 返回 cancel 不弹 toast 属正常（用户取消分享无反馈），测试用劫持 YijingShare.share 计数验证
- 清理：删除 .syntax-check.js 与 debug-*.js；sw.js → v1.8.0；README 更新
- 待办（下一迭代候选）：历史记录数据仅存 month/day 字符串日期，可加完整时间戳支持跨月周统计；「我的」tab 仍指向屏6，可考虑独立个人页

# 2026-08-31 第五轮迭代 — 三态布局最终打磨与矮屏密度

- 三态响应式模型落地：
  - `app` (≤900px)：6 屏横向 scroll-snap 全屏轮播，内容/顶栏/底栏限制最大 460px 居中，避免平板竖屏把 390px 设计稿拉成扁条；同时修复了 `.phone max-width:460px` 导致下一屏露到当前视口的 bug
  - `grid` (901–1599px)：390×844 原尺寸换行网格 + 纵向滚动
  - `fit/canvas` (≥1600px)：2940×1300 设计画廊等比缩放
  - 支持 URL `?view=app|grid|fit|canvas` 强制切换
- 修复 .phone-label 标签叠进屏内的历史 bug：JS `framePhones()` 把每屏与标签包入 `.phone-frame`，标签 `position:static` 落在屏下方
- 修复屏 1 / 屏 4 永久内容裁切：给 `.content` 增加 `scroll` 类，确保溢出内容可滚动
- 新增矮屏高密度媒体查询 `(max-width:900px) and (max-height:740px)`：压缩 hero 内边距/间距、字号、快捷入口，使 iPhone SE 375×667 首屏完整露出今日一卦 + 快捷入口；溢出从 401px 降至 218px
- 验证：Node 内联脚本语法检查通过；Playwright 7 视口诊断（iPhone SE/12/15PM、iPad mini/Pro 横竖屏、桌面）无控制台错误；首屏可见性 hero/快捷入口均 100%，无不可滚动裁切
- 清理：删除 .diag.js、.probe.js、.probe2.js、.probe3.js、.diag-*.png、.pwtest/ 临时文件
- sw.js → v1.5.0；README 更新设计系统与 URL 参数说明

# 2026-08-31 第六轮迭代 — 导航接通 + PWA 深链 + 无障碍

- 发现并修复底部 tabbar 为纯装饰的问题（无任何点击事件）：
  - 4 个 tab 增加 data-goto（今日→屏1 / 六十四卦→屏3 / 起卦→屏2 / 我的→屏6）
  - App 模式走 appGoTo 横向轮播；grid 模式 scrollIntoView 定位；fit 画廊模式对目标屏加 .flash 闪烁提示
  - 屏变更派发 yijing:screenchange 自定义事件，tab 高亮随当前屏同步（详情屏 4/5 保持原高亮）
- PWA shortcuts 深链接通：#today/#start/#history hash 导航（load 延迟 1.6s 避开骨架屏 + hashchange 实时响应）
- 无障碍：6 屏 aria-label（framePhones 注入）、tabbar role=tablist/tab + aria-selected + 键盘 Enter/Space、hero 全屏 role=dialog/aria-modal + Esc 关闭 + 开关焦点管理、fs-close role=button
- manifest 清理：移除用 icon-512 冒充 wide screenshots 的无效项
- sw.js → v1.6.0；README 补充深链说明
- 验证：Playwright 16/16 项通过（App 390×844 十项 / Grid 1180×820 三项 / Fit 1600×950 三项），三模式均无运行时错误

# 2026-08-31 第七轮迭代 — 死按钮接线 + 爻辞弹窗数据驱动

- 审计发现并修复多处「看起来可点但没绑事件」的死按钮：
  - 屏4「查看变卦推演」→ gotoScreen(4) 直达屏5；「♡」收藏按钮 → localStorage `yijing.favHexes` 持久化，openDetail 派发 yijing:detailchange 刷新收藏态
  - 屏1 最近占卜「查看全部 →」→ gotoScreen(5) 直达屏6
- 重写 05 屏爻辞 Modal（原实现两个严重问题：绑定启动时一次性绑死，render() innerHTML 重建后点击失效；数据是写死的乾/履两卦，换任何卦内容错误）：
  - 改为事件委托绑在 transformSlider 上，重渲染不失效
  - 按 _transformState 实时计算本卦/变卦，爻名/爻辞/白话全部来自 HEX_LIBRARY 真实数据
  - 动爻/受动爻徽标按 st.moving 与 yaoChanges.idx 判定
  - 「复制爻辞」clipboard API + execCommand 降级；「分享此爻」Web Share API + 复制降级
  - Esc 关闭 Modal；行 cursor:pointer 移入 CSS
- bindTabbarNav 暴露 window.YijingUI.gotoScreen 供各处复用
- sw.js → v1.7.0；README 更新交互闭环与弹窗说明
- 验证：Playwright 14/14 通过（详情跳转/收藏持久化/查看全部/Modal 真实数据一致性/复制/分享降级/Esc），无运行时错误

# 2026-08-31 易道 PWA — 64 卦数据层补全

- data.js 重写：HEX_LIBRARY 从 16 卦补全至 64 卦（卦名/拼音/卦德/上下卦/六爻爻位/六爻爻辞+白话解读）
- 修正原 1-16 卦 lines 爻位与真实卦象不符的问题（如 15 谦、16 豫）；59 涣、62 小过上下卦颠倒已修复
- 新增 HEX_EXTRA（64 卦 × 卦辞原文/大象传/全屏 intro），加载时 merge 进 HEX_LIBRARY
- index.html 修复：画廊"第17-64卦"提示语在数据完整后不再渲染；getYaoName 前缀 bug（九九→九二）；hero 全屏解读与卦辞改用真实卦数据（原 DEFAULT_QUOTES 只覆盖 5 卦）
- sw.js 缓存版本 v1.0.0 → v1.1.0；README 补充数据层说明
- 校验方法：Python 正则校验（64 卦序/lines 与三爻卦象一致性/爻名与阴阳一致性）+ Node 沙箱执行验证 merge；注意 bash heredoc 会把反斜杠转为正斜杠，含正则的脚本要用 Write 工具落盘
- 待办（下一迭代候选）：04 卦辞解析屏仍为乾卦静态 HTML，可改为按 HEX_LIBRARY 动态渲染；历史记录 06 屏可接 localStorage 真实起卦记录

# 2026-08-31 第二轮迭代 — 04 屏卦辞解析动态化
- 04 卦辞解析屏从乾卦静态 HTML 改为按 HEX_LIBRARY 动态渲染：hero（爻位图/卦名/序号中文数字/卦辞引文）+ 三 Tab（卦辞=原文+intro 白话；爻辞=六爻卡片，动爻高亮；象传=大象传+卦德）+ 个性化建议卡 + 六爻解读列表（保留折叠开关节点，仅替换行）
- 入口接线：03 屏卦卡（data-no 委托点击）、06 屏历史卡（data-hex，普通点击打开，长按多选/多选模式不受影响）→ window.YijingUI.openDetail(no)，04 屏朱红光圈闪烁提示
- 新增 URL 调试参数 hex=1-64 直接渲染指定卦；新 API：openDetail / renderDetailHex
- sw.js → v1.2.0；README 更新
- 校验：内联脚本 new Function 语法检查通过（75240 字符单块），7 个新 DOM 挂载点齐全
- 待办：05 屏变卦推演的爻变结果卦目前是静态文案，可改为真实变卦计算（动爻取反→查变卦序）；起卦流程（02 屏）尚未接通到"推演中"骨架屏→04 屏的闭环

# 2026-08-31 第三轮迭代 — 05 变卦推演引擎 + 02 起卦闭环 + 06 历史持久化
- data.js 新增 YijingEngine 推演引擎：
  - 64×6 爻位反查（二进制 key）
  - 铜钱起卦（概率 12.5/37.5/37.5/12.5）/ 蓍草大衍（6.25/31.25/43.75/18.75）/ 数字起卦（先天数）
  - transform(benNo, moving)：真实变卦 + 互卦 + 体用生克（五行生克：用生体/用克体/体克用/体生用/比和）
- 05 屏变卦推演从静态「乾→履」改为全动态：
  - 本卦/变卦/推演三视图数据驱动
  - 差异行：上卦/下卦/动爻/互卦/体用/吉凶
  - 点击爻切换动爻，变卦实时重算
  - 默认状态 乾(1)+九三动 → 履(10)，保持视觉设计稿的变卦
- 02 屏起卦闭环：
  - method-card 加 data-method
  - 点击数字/蓍草/铜钱起卦 → 校验问题 → 推演中 skeleton → 生成卦象 → 05 屏同步 + 写入 localStorage + 04 屏渲染结果 + 06 屏刷新
  - 01 屏快捷入口：随机起卦直接走铜钱流程；手动起卦聚焦 02 屏输入框
- 06 屏历史记录接 localStorage：
  - YijingHistory 模块：load/save/add/all/removeMany；真实记录在前，内置示例在后
  - 月份导航随数据动态扩展（最早记录月份 ~ max(最新记录, 当前月)）
  - 删除同时生效于 localStorage
- 修复全站卦象朝向 bug：画廊/历史/详情/hero 均按「上爻在上、初爻在下」绘制（之前数组顺序直接渲染导致屯卦显示成蒙卦）
- 修复 window.YijingUI = window.YIjingUI 拼写错误，避免 API 被清空
- 新增 URL 调试参数：hex=1-64 打开解析；hex=3&moving=1,4 直接推演
- sw.js → v1.3.0；README 更新
- 校验：Node 语法检查 + Playwright 18 项冒烟测试（0 控制台错误）+ 同 context 重启 persistence 验证

# 2026-08-31 第四轮迭代 — 界面设计审查与移动优先完善
- 审查结论：原页面是 2940×1300 设计画廊经 JS 等比缩放，390px 视口下 scale≈0.13，40px 按钮仅 5px、11px 文字 1.5px，未适配移动端；无 @media 布局，无 safe-area/dvh/tap-highlight，font-size 多达 17 档且含 8/9px 不可读中文
- 新增设计令牌：--font-serif/--font-sans/--font-num 中文回退；字号比例 fs-nano 至 fs-display；行高 1.3/1.5/1.75/1.9；--tap-min 44px；safe-area-inset 变量；--text-tertiary #94836a (4.66:1)、--cinnabar-text #d9614f (4.70:1)
- 全局移动基础：100dvh、-webkit-tap-highlight、touch-action:manipulation、overscroll-behavior:none、:focus-visible、font-feature-settings
- App 模式媒体查询：窄屏/触屏自动切换为全屏单屏，6 屏横向 scroll-snap 轮播，1:1 铺满真实视口；关闭 fitViewport 缩放；顶部屏序指示器 + 首屏滑动提示；屏内返回箭头 ← 可回到上一屏
- 触控目标修复：icon-btn 40→44px；tab 最小 44×48；ind-arrow 视觉 28px + ::after 扩展 44px 命中区
- 排版治理：52 处字体族收敛到变量；8px/9px 共 12 处抬到 10/11px；16 处承载文字的 --text-faint 改为 --text-tertiary；4 处小字朱红改为 --cinnabar-text
- 安全区：status-bar、tabbar、hero-fullscreen、app-pager 均响应 safe-area；底部指示器避让 Home Indicator
- 验证：Node 语法 + Playwright 30/30 项测试通过（390×844 App 模式 / 桌面画廊双态 / 滑动 / 触控 / 对比 / 无运行时错误）
- sw.js → v1.4.0；README 更新；输出 preview-mobile-home.png、preview-mobile-detail.png、preview-desktop.png

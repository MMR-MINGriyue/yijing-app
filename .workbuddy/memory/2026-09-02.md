# 2026-09-02 第二十七轮迭代 — 屏 4 卦辞解析 hero 紧凑化 (v1.24.0)

## 上下文

iter26 (v1.23) 修复了屏 5 变卦推演 3 view 切换 bug, 推送后 Actions success, Release v1.23.0 (2026-09-02T01:31:06Z).

## iter27 设计优化

### 问题
7 屏截图诊断发现 屏 4 detail-hero 高度 277px 偏大, 挤压后续内容:
- tabs y=435 (首屏中段位置)
- 「个性化建议」text-card 在 tab-panes 之外, top=859 完全在视口外
- content scrollH=1034 > clientH=782, 用户必须滚动 250px 才能看到建议

### 修复
.detail-hero CSS 紧凑化:
```css
.detail-hero {
  padding: 16px 20px; /* 28/24 → 16/20 (省 24px) */
  gap: 8px;            /* 14 → 8 (省 24px 累计 4 个 gap) */
  border-radius: 20px; /* 24 → 20 */
}
.detail-hero .hex-mini { width: 54px; height: 66px; gap: 3px; }  /* 60×84 → 54×66 */
.detail-hero .hex-mini .hex-line { width: 48px; }                /* 54 → 48 */
.detail-hero .hex-name { font-size: 24px; }                     /* 28 → 24 */
```

### 效果
- detail-hero h: 277 → 210px (-67px, -24%)
- tabs y: 435 → 368px (上移 67px)
- 「本卦卦辞」+ 「个性化建议」两卡首屏完整可见
- content scrollH: 1034 → 967px (省 67px)

## 调试坑

1. **测试 selector 写错**: 「个性化建议」是 `#detailAdvice` (text-card featured) 在 tab-panes 之外, 不在 .tab-pane.active 内. 改测试断言为两个独立选择器
2. **爻辞 6 行的位置**: 6 张爻辞 text-card 在 .tab-pane[data-pane="yaoci"] 内, 但 6 个 yao-row 在 #detailYaoList (屏 4 通用, 不在 tab-pane 内). 改测试断言为 tab-pane 内的 .text-card 数量 = 6
3. **遗留临时 PNG 污染 git**: iter25 时期 _ls1-fixed.png / _ls2-fixed.png / _more-page.png 未清理, 这次 commit 差点一起提交. git reset HEAD + rm 解决

## 验证

- iter27-detail-hero.js: **13/13 通过** (hero 尺寸/padding/gap/卦名/hex-mini 6 项 + tabs 上移 + 2 张卡首屏可见 + 3 个 tab 切换回归 + 零 pageerror)
- iter17-iter27 全套 **234/234 零失败** (15+16+12+44+49+14+11+25+22+13+13)
- 语法检查通过
- 推送 main → Actions run 33583670779 completed/success → **Release v1.24.0** (2026-09-02T02:36:53Z, APK 已 attach)

## 文件变更

- index.html: .detail-hero / .detail-hero .hex-mini / .hex-name CSS 紧凑
- sw.js: v1.23.0 → **v1.24.0**
- README.md: 设计系统新增「屏 4 卦辞解析 hero 紧凑化 (v1.24)」节
- www/index.html, www/sw.js, www/README.md: 同步 root
- .pwtest/iter27-detail-hero.js: 新增 13 项测试

## 状态

v1.24.0 / 27 轮迭代, 4 态布局 + 7 屏 + 4 大占法全部稳定. 后续可选:
- 屏 2 起卦表单分步引导 (大运流年占满屏)
- 历史屏搜索高亮
- 离线策略细化 (SW stale-while-revalidate)
- tab-panes min-height 100px 与 pane 实际 151px 不一致 (overflow visible 但容器记录是 min-height, 视觉 OK)